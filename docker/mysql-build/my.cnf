[mysqld]
innodb_ft_min_token_size = 2
ft_min_word_len = 2

# Disable standard stop words (you can set your own)
innodb_ft_enable_stopword = OFF

# Encoding setting
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci